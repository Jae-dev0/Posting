services:
  web:
    build:
      context: .
      secrets:
        - npm-token
    restart: unless-stopped
    ports:
      - '${DOCKER_APP_PORT}:80'

secrets:
  npm-token:
    file: ./docker/node/npm-token.txt
